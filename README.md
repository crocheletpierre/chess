Chess game aimed at developing and playing with agents.
The source code is organized as follow:
- `chess` the code for game itself
- `tests` tests for the `chess` folder
- `agents` the code for the agents, with each agent in its own folder.
- `main.py` the entrypoint, one can launch a game with `uv run main.py`