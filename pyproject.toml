[project]
name = "chess"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.3.2",
    "pydantic>=2.11.7",
    "termcolor>=3.1.0",
]

[project.scripts]
chess = "chess.main:main"

[tool.uv]
package = true
